질문
1) canary 삽입으로 인한 오버플로우 회피에서 canary의 값은 랜덤 한 값을 써 저장을 하는가?
2) FLOATING POINT가 많은 bit수를 가지고 무엇을 하는지 잘 모르겠습니다.
3) movaps 설명 할때 어디쪽을 말씀하시는지 잘모르겠습니다. 다시 설명 부탁드립니다.
4) 원래는 xor인데 FLOATING POINT 레지스터의 연산이여서 pxor을 사용하는 건지 알고 싶습니다.
5) 가변크기 스택에 대해서 자세히 알고 싶습니다.


점검 문제 1.

			누가		어떻게

(1)	프로그램상의	프로그래머,	길이를 한정을 하는 라이브러리를 사용함으로써 스택의 범위를
	회피		컴파일러	침범하지 못하게 한다.

(2-1)   시스템 상의	운영체제	전체 프로그램에 대한 스택 주소를 다른 곳으로 이동을 시킴으로써
	회피				삽입 코드의 시작위치를 예측하기 어렵게 한다.

(2-2)	시스템 상의	운영체제	스택을 비실행으로 분류함으로써 스택 상 코드의 실행을 불가하게 만든다.	
	회피

(3)	canary 삽입	컴파일러	버퍼 영역을 좀 지난 스택영역에 특별한 값을 써둠으로서
					함수를 빠져나가기 전에, 스택의 오염을 검사한다.

점검 문제 2.

(1) *fp = (float) * ip
(2) *dp = (double) * ip
(3) *lp = (long) *ip
(4) *ip = (long) *ip